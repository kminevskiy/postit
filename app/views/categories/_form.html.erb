<%= render "shared/errors", errors: @category.errors %>

<%= form_for @category do |f| %>
  <%= f.label :name %>
  <%= f.text_field :name %>
  <%= f.submit submit_title, class: "btn btn-primary" %>
<% end %>
